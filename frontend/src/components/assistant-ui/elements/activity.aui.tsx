"use client";

// Activités de test retirées — le point d'entrée unique est désormais
// le popover @mentions (deep-research, agenda, code, exercise…).
// Ce module est conservé à vide pour éviter une casse d'import ; il
// sera supprimé quand plus aucun import ne le référence.
export function ActivityTrigger() {
  return null;
}
