# Core API
fastapi
uvicorn[standard]
sse-starlette
pydantic
python-dotenv
requests
PyYAML

# Auth
pyjwt
cryptography

# LangChain / LangGraph
langchain
langchain-core
langchain-ollama
langgraph
langgraph-checkpoint-sqlite
langsmith

# Providers & Search
ollama
tavily-python

# Realtime
livekit-api
# Agent tuteur vocal — worker STT/LLM/TTS via LiveKit Inference.
# Versions pinnées au moment de l'ajout ( livekit-agents 1.8.2 ).
livekit-agents>=1.8.2
livekit-plugins-silero>=1.8.2

# Data / RAG
sqlalchemy
beautifulsoup4
pymupdf          # extraction PDF ( app/rag/documents.py — import fitz )

# PostgreSQL ( Neon ) — obligatoire des que DATABASE_URL est definie.
# psycopg 3 en mode binaire : pas de compilation ( contrairement a psycopg2 ).
# Sert aussi au checkpointer LangGraph ( langgraph-checkpoint-postgres ).
psycopg[binary]>=3.2
